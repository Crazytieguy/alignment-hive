# Per-dev overrides (secrets and generated values).
# Non-secret defaults are in .env (checked in).

# Generated by scripts/setup-web.sh
ALIGNMENT_HIVE_CONVEX_URL=https://your-deployment.convex.cloud

# RunPod API key (required for remote-kernels MCP server)
# Get yours at https://runpod.io/console/user/settings
RUNPOD_API_KEY=
