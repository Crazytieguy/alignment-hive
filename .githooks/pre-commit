#!/bin/bash
set -e

# Rebuild CLI bundle if source changed
if git diff --cached --name-only | grep -q "^packages/hive-cli/src/"; then
    echo "hive-cli source changed, rebuilding..."
    ROOT="$(git rev-parse --show-toplevel)"
    bun run --filter '@alignment-hive/hive-cli' build
    if ! git diff --quiet plugins/hive-mind/cli.js 2>/dev/null; then
        echo "Staging updated cli.js..."
        git add plugins/hive-mind/cli.js
    fi
fi
